package main

import (
	"github.com/go-telegram-bot-api/telegram-bot-api"
	"log"
)

const myId = 227605930
const botToken = "371494091:AAGndTNOEJpsCO9_CxDuPpa9R025Lxms6UI"

func main() {
	bot, err := tgbotapi.NewBotAPI(botToken)
	if err != nil {
		return
	}

	_, err = bot.Send(tgbotapi.NewMessage(57813058, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(244489778, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(129363483, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(238697588, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(248239658, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(243429867, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(185802556, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(107950408, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(149906245, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(94943173, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(258540109, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(218567363, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(204767177, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}

	_, err = bot.Send(tgbotapi.NewMessage(200874470, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(693712, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(338030847, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(142080444, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(245090894, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}

	_, err = bot.Send(tgbotapi.NewMessage(250493282, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(258816282, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(221524772, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(70167980, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(270519216, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(61219035, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(245647624, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(254438520, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(1469626, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(227605930, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(200867264, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(172833377, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(161872635, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(215065513, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))
	if err != nil {
		log.Print(err)
	}
	_, err = bot.Send(tgbotapi.NewMessage(45832584, "Советую посетить раздел \"/help setgroup\", чтобы понять принцип работы меток."))

	bot.Send(tgbotapi.NewMessage(myId, "Версия Помощника обновлена.\n\n ->  Добавлена возможность запоминать несколько групп одновременно, чтобы можно было быстро получить доступ к расписанию чужой группы.\n\n"+
		"-> Обновлен раздел /help, так же добавлен help по каждой команде."))

}
